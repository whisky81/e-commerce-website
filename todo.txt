sse
    GET /api/v2/products - products list 
    GET /api/v2/products/:id - product detail (?)
    GET /api/v2/orders - orders list 
    GET /api/v2/orders/my - my orders 
    GET /api/v2/products/:id/reviews
frontend
    chỉnh lại giao diện quá xấu chữ quá nhỏ, không nền chèn tiếng anh tiếng việt xen kẽ 
    review
    add new addr 
    hiển thị số lượng sản phẩm đã bán của 1 product 
    chia từng pages cho collection??
quản lý stock 
thêm chức nưng thanh toán khác (eg. momo, vnpay, ...)
delivery fee theo địa chỉ (gg map)
thông báo và xác thực email khi người dùng đk (không thể thao tác nếu chưa xác thực)
quảng cáo bằng email 
bổ sung model Brand (?)
models tham khảo: https://github.com/manhblue04/E-Commerce
admin chỉnh sửa  + bổ sung ảnh hero
danh sách yêu thích  
thống kê phía admin
giảm giá
sau khi review create thif đưa vào response để append 
---
hướng dẫn chạy
    git clone ...
    trong thư mục backend
        npm i 
        thêm file           .env 
        chạy file:          npm run v2
    trong thư mục admin
        npm i
        thêm file           .env 
        chạy file:          npm run dev
    trong thu mục frontend
        npm i 
        thêm file           .env
        chạy file:          npm run dev

lưu ý: tìm sửa xóa những sản phẩm nhiều tiếng anh



# frontend
- `cart bug`: chọn 1 sản phẩm nhưng ở 
        phần thưc hiện chọn thanh toán giá là tổng sản phẩm trong giỏ